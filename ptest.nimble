# Package

version       = "0.1.0"
author        = "Andre von Houck"
description   = "You debug with print statements? Why don't you also test with them?"
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 1.0.0"

bin = @["ptest"]
